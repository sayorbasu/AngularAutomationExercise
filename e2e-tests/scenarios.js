'use strict';

/* https://github.com/angular/protractor/blob/master/docs/toc.md */

describe('automation exercises app', function() {


  /*it('should have a heading', function() {
    browser.driver.manage().deleteAllCookies();
    browser.ignoreSynchronization = true;
    browser.get('index.html#/gettingstarted');
    expect(element(by.css('.h1')).isPresent());
  });

    it('should be able to click on alert button', function() {
      browser.driver.manage().deleteAllCookies();
      browser.ignoreSynchronization = true;
      browser.get('index.html#/exercise1/partA');
      element(by.id('.md-primary md-raised md-button md-ink-ripple')).click();
    });*/
});
